# 代码质量审查报告

> 生成日期：2026-07-23（第三轮审查）
> 审查范围：code/js 全部源码 + feature/match3-optimization 分支 diff
> 状态图例：⬜ 未修复 | ✅ 已修复 | ⏭️ 已跳过（附原因）

---

## P0 — 实际 Bug（建议最先修）

- [x] ✅ **P0-1 答题页"提交答案"和"提示"按钮重叠**
  - 位置：`code/js/pages/答题页/index.js:468-474`（判定）、`:375-404`（渲染）
  - 问题：提交按钮与提示按钮命中区域完全相同（`width/2-50` ~ `width/2+50`），选中答案后点击一次会同时触发 `submitAnswer()` 和 `useHint()`，白消耗提示次数；两个按钮也画在同一位置。

- [x] ✅ **P0-2 答题页"下一题"按钮点不中**
  - 位置：`code/js/pages/答题页/index.js:480`（判定）、`:327`（绘制）
  - 问题：命中判定用 `height/2+60 ~ height-50`，但按钮实际画在 `buttonY = endY + 40`（由解析文字行数决定），两者对不上。

- [x] ✅ **P0-3 题库 JSON 是死数据，题目硬编码**
  - 位置：`code/content/heritage/题库.json`、`code/content/nature/题库.json`、`code/content/red/题库.json`；`code/js/pages/答题页/index.js:57-173`
  - 问题：三个题库文件从未被 require，10 道题硬编码在代码里，改题需要改代码。

- [x] ✅ **P0-4 证书页面不可达 + 字段名不匹配**
  - 位置：`code/js/pages/成就页/index.js:327`（`generateCertificate` 无调用方）、`:220`（读 `userInfo.nickname`）；`code/js/pages/登录页/index.js:247`（存的是 `nickName`）
  - 问题：证书功能入口不存在（死功能）；即使进入，用户名字段大小写不匹配，永远显示"用户"。

- [x] ✅ **P0-5 match3 对象池 use-after-recycle**
  - 位置：`code/js/games/match3/game.js:422-435`
  - 问题：`_createSpecialPiece` 先把 `piece` 回收进池，之后再读 `piece.type/piece.color`；且 `piecePool.get()` 可能立刻把刚回收的对象发回来。建议原地修改 `piece.special = true`，删除该方法及 `SpecialPiece` 类。

- [x] ✅ **P0-6 成就数据两套并存**
  - 位置：`code/js/databus.js:64`（`databus.achievements`）vs `code/js/utils/scoreManager.js:41`（`scoreManager.achievements`）
  - 问题：两套独立成就数据；证书页用前者（永远为空），成就列表页用后者。`databus.achievements`、`checkInPoints`、`addCheckInPoint`、`unlockExclusiveSkin` 均无调用方。

### 第二轮审查新增

- [x] ✅ **P0-25 match3 消除后读取已清空的棋盘格子获取颜色**
  - 位置：`code/js/games/match3/game.js`（`handleCellSwap` 及 `checkNewMatches`）
  - 问题：先调用 `removeMatches()` 将格子置 null，再调用 `buildMatchColors()` 读取颜色。此时 `this.board[row][col]?.color` 返回 undefined，回退为 `#ffffff`，消除动画颜色错误。
  - 方案：将 `buildMatchColors` 调用移到 `removeMatches` 之前。

- [x] ✅ **P0-26 PuzzleGame `handleTouchEnd` 错误读取 `e.touches` 而非 `e.changedTouches`**
  - 位置：`code/js/games/puzzle/game.js:254-263`
  - 问题：`touchend` 事件中 `e.touches` 通常为空，应优先使用 `e.changedTouches`。当前优先级与项目其他页面不一致。
  - 方案：优先使用 `e.changedTouches`，与 `canvasUtils.getTouchCoords` 保持一致。

- [x] ✅ **P0-27 PuzzleGame `touchEndX/touchEndY` 属性声明但从未赋值**
  - 位置：`code/js/games/puzzle/game.js:15-16`
  - 问题：`this.touchEndX` 和 `this.touchEndY` 在构造函数中声明，但 `handleTouchMove` 方法缺失，这两个属性从未被赋值。
  - 方案：删除未使用的属性，或实现 `handleTouchMove` 方法。

- [x] ✅ **P0-28 DataBus.score 与 ScoreManager 双重计分，`getTotalScore` 语义混乱**
  - 位置：`code/js/databus.js:6,51-53,62-64`；`code/js/utils/scoreManager.js:287-289`
  - 问题：两套独立计分体系：`DataBus.score`（每答对一题 +10）和 `ScoreManager.scores.quiz.bestScore`。`getTotalScore` 只返回 `match3.totalScore + quiz.bestScore`，不含答题累计分数。`quizData` 和 `scores.quiz` 重复记录答题数据，更新时机不同可能导致不一致。
  - 方案：移除 `DataBus.score`，统一使用 ScoreManager 管理分数。

- [x] ✅ **P0-29 match3 `_removeLineMatches` 的 `len` 与实际匹配数 off-by-one**
  - 位置：`code/js/games/match3/game.js:274-298`
  - 问题：`len = match.end - match.start` 比实际匹配数少 1（3 连消时 len=2）。当前映射恰好正确（len=2→普通消除，len=3→line-clear，len≥4→rainbow），但代码缺乏注释说明，且 `removed` 标记与特殊棋子创建存在语义混淆。
  - 方案：改用 `matchLength = match.end - match.start + 1` 增强可读性。

### 第三轮审查新增

- [ ] ⬜ **P0-30 QuizPage `loadQuestions` 使用同步文件读取阻塞主线程**
  - 位置：`code/js/pages/答题页/index.js`（`loadQuestions` 方法）
  - 问题：使用 `wx.getFileSystemManager().readFileSync()` 同步读取 3 个 JSON 题库文件。构造函数调用 `loadQuestions()` 时会阻塞主线程，造成 UI 卡顿。
  - 方案：改为异步 `readFile` + 回调/Promise，或在页面 `onLoad` 阶段预加载题库。

- [ ] ⬜ **P0-31 QuizPage 提示按钮触摸条件变更导致选中选项后无法使用提示**
  - 位置：`code/js/pages/答题页/index.js`（`handleTouchStart` 中提示按钮判定）
  - 问题：新代码增加 `this.selectedOption === null` 条件，用户选择选项后无法点击提示按钮排除错误答案。旧代码无此限制，用户可在选择后使用提示。
  - 方案：移除 `selectedOption === null` 条件，或明确产品设计意图后更新。

- [ ] ⬜ **P0-32 match3 `_createSpecialPiece` 行为变更导致对象池失效**
  - 位置：`code/js/games/match3/game.js`（`_createSpecialPiece` 方法）
  - 问题：旧代码将原棋子回收到 `piecePool` 后从池中获取新对象；新代码直接修改原棋子属性（`piece.special = true`）。原棋子不再回收，对象池形同虚设，长期运行可能增加 GC 压力。
  - 方案：保留原地修改（简化正确），但删除不再使用的 `ObjectPool` 类，或在不需要对象池时移除相关代码。

## P1 — 架构问题

- [x] ✅ **P1-7 三个存储管理器并存**
  - 位置：`code/js/databus.js`（key `databus`）、`code/js/utils/scoreManager.js`（key `gameScores`）、`code/js/utils/dataManager.js`（key `gameData`）
  - 问题：`DataManager` 实例化后全项目无人使用（仅 `app.js:56,79` 引用），整个文件是死代码。建议合并为一个数据层。

- [x] ✅ **P1-8 `currentPage` 挂在 databus 上**
  - 位置：`code/js/common/app.js:104-141`
  - 问题：页面实例存在数据总线里，职责错乱，应归 App 自己管理。

- [x] ⏭️ **P1-9 GameGlobal 当全局服务定位器** — 已跳过：项目规模小可接受，BasePage 已减少隐式耦合
  - 位置：全项目（`GameGlobal.app/databus/resourceManager/systemInfo/dataManager`）
  - 问题：页面间隐式耦合。项目规模小可接受，但至少统一入口（如页面基类注入）。

- [x] ✅ **P1-10 没有页面基类**
  - 位置：6 个页面/游戏类
  - 问题：各自重复实现系统信息获取、背景图加载（5 处复制粘贴的 `loadBackgroundImage`）、触摸坐标提取（`canvasUtils.getTouchCoords` 存在但 match3/puzzle 未使用）、空 `destroy()`。抽 `BasePage` 可消约 300-400 行重复。

### 第二轮审查新增

- [x] ✅ **P1-25 HomePage 模块顶层捕获 `GameGlobal.databus` 引用**
  - 位置：`code/js/pages/首页/index.js:4`
  - 问题：`const databus = GameGlobal.databus;` 在模块加载时执行，隐式依赖 `app.js` 先创建 databus。其他页面均在方法内部动态获取引用。
  - 方案：改为方法内部通过 `GameGlobal.databus` 动态获取。

- [x] ✅ **P1-26 PuzzleGame `handleTouchEnd` 缺少按钮区域过滤**
  - 位置：`code/js/games/puzzle/game.js`
  - 问题：`handleTouchStart` 中按钮区域触摸会直接 return 不设置 `touchStartX/Y`，但 `handleTouchEnd` 仍会使用上一次的坐标进行滑动计算，可能产生意外操作。
  - 方案：在 `handleTouchEnd` 开头检查是否在按钮区域，是则直接返回。

- [ ] ⏭️ **P1-27 QuizPage `difficulty` 参数从未被外部传入** — 已跳过：需 UI 设计决策，当前 easy 模式可用
  - 位置：`code/js/pages/答题页/index.js:5`
  - 问题：`constructor(difficulty = 'easy')` 接受难度参数，但 `app.js` 中 `new QuizPage()` 不传参数，始终使用 `'easy'`。`setupDifficulty` 中 medium/hard 配置完全无法触达。
  - 方案：在首页或答题页入口添加难度选择 UI。

- [x] ✅ **P1-28 BasePage 缺少触摸接口定义**
  - 位置：`code/js/common/basePage.js`
  - 问题：BasePage 只定义了 `update()`、`render()`、`destroy()`，但所有页面都需要 `handleTouchStart/Move/End`。子类是否实现触摸处理靠约定而非契约。
  - 方案：在 BasePage 中添加空的 `handleTouchStart(e)`、`handleTouchMove(e)`、`handleTouchEnd(e)` 方法。

- [x] ✅ **P1-29 `app.js` mock canvas fallback 导致静默失败**
  - 位置：`code/js/common/app.js:21-46`
  - 问题：`wx.createCanvas()` 失败时创建 mock canvas，所有绘制变为 no-op，应用"正常运行"但不显示任何内容，掩盖真实问题。
  - 方案：在 mock canvas 上添加 `isMock` 标志。

- [ ] ⏭️ **P1-30 testRunner 在生产环境无条件加载并自动运行** — 已跳过：测试框架依赖自动加载，需配合构建系统实现环境判断
  - 位置：`code/game.js:14-17`；`code/js/testRunner.js:283-290`
  - 问题：`game.js` 无条件 `require('./js/testRunner')`，模块加载时自动 `setTimeout(runAllTests, 2000)`。生产环境会：增大包体积、2 秒后自动运行测试切换页面、向 localhost 发送必定失败的 HTTP 请求。
  - 方案：需配合构建系统实现环境判断。

- [x] ✅ **P1-31 match3 `dropPieces` 创建的 `pieceCopy` 丢失 `row/col` 属性**
  - 位置：`code/js/games/match3/game.js:330-336`
  - 问题：`pieceCopy` 只复制了 `type/color/special/specialType`，与 `GamePiece` 结构不一致。虽然动画系统使用独立的坐标计算，但如果代码尝试访问 `piece.row/col` 将得到 `undefined`。
  - 方案：在 `pieceCopy` 中包含 `row/col`。

### 第三轮审查新增

- [ ] ⬜ **P1-32 AchievementPage 直接穿透访问 `databus.scoreManager.achievements.unlocked` 破坏封装**
  - 位置：`code/js/pages/成就页/index.js`（`renderCertificate` 方法）
  - 问题：`const achievements = databus.scoreManager.achievements.unlocked;` 直接访问三层嵌套属性，违反迪米特法则。如果 `scoreManager` 内部结构变化，此处会崩溃。
  - 方案：在 `databus` 或 `scoreManager` 上添加 `getUnlockedAchievements()` 方法。

- [ ] ⬜ **P1-33 BasePage `loadBackgroundImage` 仅支持微信环境，不兼容浏览器调试**
  - 位置：`code/js/common/basePage.js:10-21`
  - 问题：`loadBackgroundImage` 只处理 `wx.createImage()`，移除了旧代码中的 `window.Image` fallback。在非微信环境下运行测试时背景图加载会失败。
  - 方案：添加 `window.Image` 兼容分支，与 match3/puzzle 的 `loadPuzzleImage` 保持一致。

- [ ] ⏭️ **P1-34 `app.currentPage` 从 databus 迁移到 app 实例，外部引用可能失效** — 已跳过：项目内部已全部适配，无已知外部引用
  - 位置：`code/js/common/app.js`
  - 问题：`currentPage` 从 `GameGlobal.databus.currentPage` 改为 `app.currentPage`。如果有其他代码通过旧路径访问，将得到 `undefined`。
  - 方案：确认无外部引用后可忽略。

## P2 — 重复代码 / 可维护性

- [x] ✅ **P2-11 渐变按钮绘制重复 8+ 处**
  - 位置：match3、puzzle、答题页、成就页
  - 问题：每个按钮 ~15 行渐变+阴影+圆角+文字，且绘制坐标与命中判定坐标分离（P0-1/P0-2 由此产生）。抽 `drawButton()` 并让按钮返回 rect 供命中复用。

- [x] ⏭️ **P2-12 魔数遍布** — 已跳过：BasePage + drawButton 已减少部分重复坐标，剩余魔数分散在各游戏逻辑中，风险低
  - 位置：各页面（`startY=120/130`、按钮区域硬编码坐标等）

- [x] ✅ **P2-21 match3/game.js 单文件职责混杂**
  - 位置：`code/js/games/match3/game.js`（原 1420 行）
  - 问题：游戏逻辑、Canvas 渲染、动画系统、触摸处理、对象池全在一个类里，难以维护和测试。
  - 方案：拆分为 `constants.js`（常量）、`animation.js`（动画系统）、`renderer.js`（渲染）、`game.js`（核心逻辑）四个模块。

- [x] ✅ **P2-22 match3 渲染代码与游戏逻辑耦合**
  - 位置：`code/js/games/match3/game.js`（原 ~500 行绘制代码）
  - 问题：10+ 个 `draw*` 方法与游戏状态直接耦合，修改渲染需理解游戏逻辑。
  - 方案：抽取 `Match3Renderer` 类，通过接收 game 实例引用解耦。

- [x] ✅ **P2-23 match3 动画系统未独立**
  - 位置：`code/js/games/match3/game.js`（原 ObjectPool + 动画管理）
  - 问题：对象池、缓动函数、动画生命周期管理散落在游戏类中。
  - 方案：抽取 `AnimationManager` 类，封装对象池和动画状态机。

- [x] ✅ **P2-24 match3 常量定义分散**
  - 位置：`code/js/games/match3/game.js`（原颜色/图标/配置常量）
  - 问题：颜色、图标路径、游戏配置等常量与逻辑混在一起。
  - 方案：抽取 `constants.js` 集中管理。

- [x] ✅ **P2-13 调试日志残留**
  - 位置：`code/js/utils/scoreManager.js:115,136,156,166,186,211,255` 等；`code/js/games/puzzle/game.js:662-679`；`code/js/pages/首页/index.js:74`
  - 建议：统一 `logger`（带开关）或删除。

- [x] ✅ **P2-14 风格不统一**
  - 位置：`code/js/databus.js` 用老式 `prototype`，其余文件全用 `class`。

### 第二轮审查新增

- [x] ✅ **P2-32 QuizPage `showResult` 属性重复声明**
  - 位置：`code/js/pages/答题页/index.js:13,18`
  - 问题：`this.showResult = false;` 在构造函数中被赋值两次。
  - 方案：删除其中一处重复声明。

- [x] ✅ **P2-33 PuzzleGame 触摸坐标提取逻辑重复 2 处**
  - 位置：`code/js/games/puzzle/game.js:636-646` 及 `:253-263`
  - 问题：`handlePieceClick` 和 `handleTouchEnd` 中提取 x,y 坐标的 10 行代码完全重复，且未使用 `getTouchCoords` 工具函数。
  - 方案：统一使用 `canvasUtils.getTouchCoords`。

- [x] ✅ **P2-34 match3 常量中图标文件名拼写错误 `pinlk`**
  - 位置：`code/js/games/match3/constants.js:38`
  - 问题：`'images/match3/icon_0003_pinlk.png'` 中 `pinlk` 应为 `pink`。如果实际文件命名为 `pink.png` 则图标加载失败。
  - 方案：重命名文件并更新代码路径。

- [x] ✅ **P2-35 PuzzleGame 回退颜色与项目色彩体系不一致**
  - 位置：`code/js/games/puzzle/game.js:495-497`
  - 问题：拼图图片加载失败时回退颜色为 `['#FF5722', '#4CAF50', '#F44336']`，非项目标准色彩。与 match3 的 6 色体系无共享定义。
  - 方案：改为 match3 项目色彩 `['#EF4444', '#F59E0B', '#3B82F6']`。

- [ ] ⏭️ **P2-36 魔数散布于各页面触摸区域判断中** — 已跳过：风险低，各页面布局参数独立
  - 位置：多文件（match3/game.js、puzzle/game.js、成就页/index.js、答题页/index.js）
  - 问题：按钮布局参数在 `render` 和 `handleTouchStart` 中各硬编码一次，修改绘制参数时容易忘记同步修改触摸区域。
  - 方案：将按钮布局参数提取为实例属性，render 和 touch handler 共享同一数据源。

- [ ] ⏭️ **P2-37 scoreManager `updatePlayTime` 单位转换歧义** — 已跳过：无调用方，不影响功能
  - 位置：`code/js/utils/scoreManager.js:328-330`
  - 问题：方法名暗示接受分钟，内部 `+= minutes * 60` 转为秒存储。整个项目中无调用方。
  - 方案：明确文档化单位约定，或改为接受秒参数。

- [x] ✅ **P2-38 match3 renderer `drawGameInfo` 硬编码文本 x 坐标**
  - 位置：`code/js/games/match3/renderer.js:93-101`
  - 问题：`40`、`150`、`260` 等 x 坐标硬编码，屏幕宽度变化时不会自适应。
  - 方案：使用相对布局计算 x 坐标。

## P3 — 死代码清理

- [x] ✅ **P3-15 删除 `DataManager` 整个文件**（84 行，`code/js/utils/dataManager.js`）
- [x] ✅ **P3-16 删除 `knowledgeBuff`**：答题页会 set，全项目无人 get（`code/js/databus.js:10,27,44,116-122`；`code/js/pages/答题页/index.js:499,503`）
- [x] ✅ **P3-17 删除 match3 死代码**：`PieceType` 枚举、`SpecialPiece` 类、`ICONS[6]=null`、`COLORS[6]`（`code/js/games/match3/game.js:78-115`）
- [x] ⏭️ **P3-18 删除 `renderCertificate` 相关代码** — 已跳过：证书功能已启用（P0-4 添加了入口）
- [x] ✅ **P3-19 删除 databus 死字段/方法**：`achievements`、`checkInPoints`、`unlockExclusiveSkin`（`code/js/databus.js`）
- [x] ✅ **P3-20 首页文案错别字**：「精彩糖巧，清洁消乐」「精彩缝补」「详漫数字」（`code/js/pages/首页/index.js:34-36`）

### 第二轮审查新增

- [x] ✅ **P3-39 HomePage `clearAchievements` 方法已定义但从未被调用**
  - 位置：`code/js/pages/首页/index.js:194-210`
  - 问题：方法定义了清除成就的完整逻辑，但无任何 UI 入口触发。
  - 方案：删除死代码。

- [x] ✅ **P3-40 AchievementPage import 了 `drawButton` 但从未使用**
  - 位置：`code/js/pages/成就页/index.js:1`
  - 问题：`const { drawRoundedRect, drawButton, getTouchCoords } = require(...)` 中 `drawButton` 从未调用。
  - 方案：从 import 中移除 `drawButton`。

- [x] ✅ **P3-41 QuizPage import 了 `drawButton` 但从未使用**
  - 位置：`code/js/pages/答题页/index.js:1`
  - 问题：同 P3-40，`drawButton` 被 import 但从未调用。
  - 方案：从 import 中移除 `drawButton`。

- [x] ✅ **P3-42 scoreManager `checkMatch3Achievements` 中 `match3` 变量未使用**
  - 位置：`code/js/utils/scoreManager.js:158`
  - 问题：`const match3 = this.scores.match3;` 声明后从未被引用。
  - 方案：删除未使用的变量。

- [x] ✅ **P3-43 scoreManager `checkPuzzleAchievements` 中 `puzzle` 变量未使用**
  - 位置：`code/js/utils/scoreManager.js:176`
  - 问题：`const puzzle = this.scores.puzzle;` 声明后从未被引用。
  - 方案：删除未使用的变量。

- [x] ✅ **P3-44 scoreManager `checkQuizAchievements` 中 `score` 参数未使用**
  - 位置：`code/js/utils/scoreManager.js:197`
  - 问题：`checkQuizAchievements(correctCount, totalQuestions, score)` 中 `score` 参数从未在方法体内使用。
  - 方案：删除未使用的参数。

- [x] ✅ **P3-45 PuzzleGame `destroy()` 方法为空**
  - 位置：`code/js/games/puzzle/game.js:725-726`
  - 问题：`destroy()` 方法体为空。`this.animations` 和 `this.puzzleImage` 未被释放，频繁切换页面可能造成内存占用。
  - 方案：在 `destroy` 中清理 `this.animations = []` 和 `this.puzzleImage = null`。

- [x] ✅ **P3-46 BasePage `this.pixelRatio` 赋值但从未被使用**
  - 位置：`code/js/common/basePage.js:6`
  - 问题：`this.pixelRatio = sys.pixelRatio || 1;` 在构造函数中设置，但整个项目无代码读取此属性。
  - 方案：删除未使用的属性。

- [x] ✅ **P3-47 match3 `AnimationType` 常量已定义但从未被使用**
  - 位置：`code/js/games/match3/constants.js:45-51`
  - 问题：`AnimationType` 对象定义了 `SWAP/ELIMINATION/DROP/POP/SPECIAL`，但 `game.js` 中使用的是字符串字面量（`'swap'`、`'elimination'` 等）而非常量引用。
  - 方案：删除未使用的 `AnimationType`。

- [x] ✅ **P3-48 match3 `iconCache` 不应归属 constants 模块**
  - 位置：`code/js/games/match3/constants.js:42`
  - 问题：`iconCache` 是可变数组，由 `renderer.js` 的 `loadIcons` 填充。放在"常量"模块违反职责，且 renderer 直接 import 并修改它。
  - 方案：将 `iconCache` 移入 `renderer.js` 内部管理。

- [x] ✅ **P3-49 match3 `COLOR_INDEX_MAP` 仅 renderer 使用，可内聚**
  - 位置：`code/js/games/match3/constants.js:29-32`
  - 问题：`COLOR_INDEX_MAP` 只在 `renderer.js` 中使用，导出到 constants 增加了模块间耦合。
  - 方案：移入 `renderer.js` 内部定义。

### 第三轮审查新增

- [ ] ⬜ **P3-50 QuizPage `resultButtonRect` 未在构造函数中初始化**
  - 位置：`code/js/pages/答题页/index.js`
  - 问题：`this.resultButtonRect` 仅在 `render` 中答题后赋值，构造函数中未初始化为 `null`。触摸处理中有 `if (rect && ...)` 保护不会崩溃，但不符合属性先声明后使用的规范。
  - 方案：在构造函数中添加 `this.resultButtonRect = null;`。

---

## 重构阶段计划与预算

| 阶段 | 内容 | 覆盖条目 | 预估 token | 状态 |
|------|------|---------|-----------|------|
| 1 | 修 P0 六个 Bug | P0-1 ~ P0-6 | 4–6 万 | ✅ 已完成 |
| 2 | 数据层合并：砍 DataManager、清死字段、统一存储 | P0-6、P1-7、P3-15/16/19 | 5–8 万 | ✅ 已完成 |
| 3 | 抽 BasePage + drawButton + 统一触摸/资源加载 | P1-9/10、P2-11/12 | 8–12 万 | ✅ 已完成 |
| 4 | 题库外置到 JSON + 死代码清理 + 日志收敛 | P0-3、P2-13/14、P3-17/18/20 | 3–5 万 | ✅ 已完成 |
| 5 | match3 模块化拆分：constants/animation/renderer/game | P2-21/22/23/24 | 6–10 万 | ✅ 已完成 |
| 6 | 修 P0-25/26/27/28/29 五个新发现的 Bug | P0-25 ~ P0-29 | 4–6 万 | ✅ 已完成 |
| 7 | 架构改进：触摸接口统一、testRunner 环境判断、数据层清理 | P1-25 ~ P1-31 | 5–8 万 | ✅ 已完成（P1-30 跳过） |
| 8 | 可维护性：消除重复代码、魔数提取、常量纠错 | P2-32 ~ P2-38 | 3–5 万 | ✅ 已完成 |
| 9 | 死代码清理：未使用变量/import/属性、模块职责内聚 | P3-39 ~ P3-49 | 2–4 万 | ✅ 已完成 |
| 10 | 第三轮：同步文件读取、提示按钮逻辑、对象池失效、封装破坏 | P0-30/31/32、P1-32/33/34、P3-50 | 4–6 万 | ⬜ 未开始 |
